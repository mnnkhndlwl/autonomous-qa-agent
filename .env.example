# Jira
JIRA_BASE_URL=https://your-domain.atlassian.net
JIRA_EMAIL=your-email@example.com
JIRA_API_TOKEN=

# Slack (required only when using --slack)
SLACK_WEBHOOK_URL=

# Build commands (run from project directory; use quotes for spaces)
BUILD_COMMAND_IOS=
BUILD_COMMAND_ANDROID=

# Launch commands (optional)
LAUNCH_COMMAND_IOS=
LAUNCH_COMMAND_ANDROID=

# Maestro artifacts (optional; default: <project>/maestro-artifacts)
# MAESTRO_ARTIFACTS_DIR=
